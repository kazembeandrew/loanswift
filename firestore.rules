rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      allow read, write: if request.auth.uid == userId || request.auth.token.role in ['admin', 'ceo', 'cfo', 'hr', 'loan_officer'];
    }

    match /borrowers/{borrowerId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo', 'hr', 'loan_officer'];
    }
    
    match /loans/{loanId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo', 'hr', 'loan_officer'];
    }
    
    match /{path=**}/payments/{paymentId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo', 'hr', 'loan_officer'];
    }

    match /settings/{docId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo'];
    }

    match /accounts/{accountId} {
       allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo'];
    }

    match /journal/{entryId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo'];
    }
    
    match /conversations/{conversationId} {
      allow read, write: if request.auth.uid in resource.data.participants;
    }
    
    match /conversations/{conversationId}/messages/{messageId} {
       allow read, write: if request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participants;
    }

    match /situationReports/{reportId} {
      allow read, write: if request.auth.token.role in ['admin', 'ceo', 'cfo', 'hr', 'loan_officer'];
    }
  }
}